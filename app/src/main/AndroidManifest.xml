<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="domain.com.contacts">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <application
        android:name=".application.ContactsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="domain.com.contacts.flowcontact.activity.ContactsListActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
           >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="domain.com.contacts.flowcontact.activity.ContactDetailsActivity"
            android:label="@string/contact_details"
            android:theme="@style/AppTheme.NoActionBar"
            >
        </activity>
        <activity
            android:name="domain.com.contacts.flowcontact.activity.ContactAddActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/contact_add"
            >

        </activity>
    </application>

</manifest>